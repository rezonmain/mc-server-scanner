services:
  redis:
    image: redislabs/rejson
  worker:
    build: .
    depends_on:
      - redis
    command:
      dramatiq dramatiq_actors
  scanner:
    build: .
    environment:
      - RATE
      - MONGO_URI
    depends_on:
      - redis
      - worker
    command:
      python3 -m scanner

